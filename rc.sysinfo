if [ "$PS1" ]; then
	if [ "$(uname)" == "FreeBSD" ] && hash bsdinfo 2>/dev/null; then
		bsdinfo
	elif [ "$(uname)" == "Darwin" ] && hash archey 2>/dev/null; then
		archey
	elif [ "$(expr substr $(uname -s) 1 5)" == "Linux" ] && hash screenfetch 2>/dev/null; then
		screenfetch 2> /dev/null
	fi
fi

